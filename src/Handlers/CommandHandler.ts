import { Message } from 'discord.js';
import {
    Bestfriends,
    BestfriendsGuild
} from '../deps';

export function CommandHandler(Bestfriends: Bestfriends, BestfriendsGuild: BestfriendsGuild, message: Message, args: string[], cmd: string) {

    switch(cmd) {

        

    }

}